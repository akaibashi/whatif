<p id="notice"><%= notice %></p>

<p>
  <b>What:</b>
</p>

<table>
<% @answers.each do |answer| %>
  <tr>
    <td><%= answer.what_id %></td>
    <td><%= answer.do_description %></td>
    <td><%= answer.name %></td>
  </tr>
<% end %>
</table>
<br />

<%= link_to 'Edit', edit_answer_path(@answer) %> |
<%= link_to 'Back', answers_path %>
